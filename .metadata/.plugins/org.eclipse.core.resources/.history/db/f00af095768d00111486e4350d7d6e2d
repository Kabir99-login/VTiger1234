package objectRepo;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.FindBy;
import org.openqa.selenium.support.PageFactory;
import org.testng.asserts.SoftAssert;

import genericUtility.WebDriverUtility;

public class NewProjectPage {
	WebDriver driver;
	public NewProjectPage(WebDriver d) {
		PageFactory.initElements(d, this);
		driver = d;
	}
	
	@FindBy(name = "projectName")
	private WebElement pName;
	
	@FindBy(name = "createdBy")
	private WebElement pManager;
	
	@FindBy(xpath = "//label[text()='Project Status* ']/following-sibling::select[@name='status']")
	private WebElement status;
	
	@FindBy(xpath = "//input[@value='Add Project']")
	private WebElement add;

	public WebElement getpName() {
		return pName;
	}

	public WebElement getpManager() {
		return pManager;
	}

	public WebElement getStatus() {
		return status;
	}

	public WebElement getAdd() {
		return add;
	}
	
	public void createProject(String pn, String pm, String ps) {
		pName.clear();
		pName.sendKeys(pn);
		SoftAssert sa = new SoftAssert();
		sa.assertEquals(driver.findElement(By.name("projectName")).getAttribute("value").equals(pn+12), true);
		System.out.println("Entered pn");
		
		pManager.clear();
		pManager.sendKeys(pm);
		
		WebDriverUtility wu = new WebDriverUtility();
		wu.select(status,ps);
		add.click();
		
		sa.assertAll();
	}
}
